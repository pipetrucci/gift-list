
.parallax-container
  .opacity-wrap
    .section.no-pad-bot
      .container.mr-top-ten.center
        %h3.header.center.white-text Matrimonio
        %h1.header.center.white-text.lemonada Felipe y Camila
        %hr.withe-text{:width => "40%"}
        %br
        %i.fa.fa-heart-o.center.white-text.fa-2x
        %i.fa.fa-heart-o.center.white-text.fa-2x

.section.purple.darken-1
  .valign-wrapper{style: "height: 200px;"}
    .container
      %h5.valign.center.light.white-text.arima
        %i.fa.fa-angle-double-left.white-text
        Que tu amor inagotable nos rodee, Señor, porque solo en ti está nuestra esperanza.
        %i.fa.fa-angle-double-right.white-text
      %h6.white-text.right
        Salmos 33:22

.divider

#regalos.section.scrollspy
  -#.row{style: "background: url(#{image_url("bg-pink-hearts.jpg")}) left top repeat;"}
  .row.white
    .col.m12
      %p#notice= notice
      %h3.header.center
        Lista de regalos
      .container
        %p.flow-text.center
          Estos regalos los hemos escogido cuidadosamente
          para que sean parte de nuestro hogar. Se parte tu también :D
      %br

    #show_details

    .row
      .col.offset-m1.m10
        - @regalos.each do |regalo|
          .col.s12.m6
            .card.card-gift.horizontal.hoverable.white
              .card-content
                .col.s12.m6
                  = image_tag "#{regalo.img_product.url.present? ? regalo.img_product.url : 'http://placehold.it/250x236'}", class: "img-responsive materialboxed", width: "100%"
                .col.s12.m6
                  .title-regalo
                    .light
                      = regalo.name

                  .price-text.light
                    - if !regalo.price_min.present?
                      %p
                        Precio:
                        = number_to_currency(regalo.price_max, precision: 0, separator: ".", unit: "$", format: "%u %n")
                    - else
                      %p
                        Precio:
                        = "#{number_to_currency(regalo.price_min, precision: 0, separator: ".", unit: "$", format: "%u %n")} - #{number_to_currency(regalo.price_max, precision: 0, separator: ".", unit: "$", format: "%u %n")}"


                  -if regalo.state == 1
                    -if regalo.user_id.present?
                      - user = User.find(regalo.user_id)
                      .chip.truncate
                        = image_tag user.img_contact.thumb.url
                        = user.name
                  -else
                    .chip.green.darken-1
                      .white-text
                        Disponible

                  .pd-top
                    = link_to 'Regalar!', new_user_registration_path, {:class => 'btn waves-effect waves-light teal', :method => 'get', remote: true, form: { "data-type" => "script" }}
